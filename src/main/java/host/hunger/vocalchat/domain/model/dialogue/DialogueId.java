package host.hunger.vocalchat.domain.model.dialogue;

import host.hunger.vocalchat.domain.model.shared.Identity;

public class DialogueId extends Identity {
    public DialogueId(String id) {
        super(id);
    }
}
